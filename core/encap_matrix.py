"""
core/encap_matrix.py
Generates the full 8192-variant EncapConfig matrix described in the
original spec's "Encapsulation Matrix" section. NOT needed for MVP --
config.json's "smoke" encap_mode (bare Ethernet only, see
core/packet_builder.py's DEFAULT_ENCAP) is sufficient for now. Stub only;
implement when scope expands past MVP. See README.md "MVP Scope".
"""
# TODO (post-MVP)
