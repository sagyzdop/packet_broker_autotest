"""
tests/test_lag_hash.py
LAG hash type detection (original spec "LAG Hash Type Detection" /
"LAG Hash Type Determination"). Requires 2+ DPI links to be meaningful --
MVP's dpi_lag in topology.yaml has only one interface (dpi1). Deferred.
See README.md "MVP Scope".
"""
# TODO (deferred -- needs 2+ DPI links in topology.yaml)
